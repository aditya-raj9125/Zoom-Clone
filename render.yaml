services:
  - type: web
    name: zoom-api
    runtime: python
    rootDir: apps/api
    buildCommand: pip install -r requirements.txt && pip install -e .
    startCommand: mkdir -p /tmp/zoom_data && alembic upgrade head && python scripts/seed_db.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DEBUG
        value: false
      - key: DATABASE_URL
        value: sqlite+aiosqlite:////tmp/zoom_data/zoom_clone.db
      - key: JWT_SECRET_KEY
        sync: false
      - key: FRONTEND_URL
        value: https://zoom-clone-web-gamma.vercel.app
      - key: CORS_ORIGINS
        value: https://zoom-clone-web-gamma.vercel.app,http://localhost:3000
      - key: GOOGLE_CLIENT_ID
        value: ""
      - key: GOOGLE_CLIENT_SECRET
        value: ""
